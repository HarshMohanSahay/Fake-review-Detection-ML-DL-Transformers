# Dataset

The dataset is not included in this repository.

Expected file:

```text
data/final_reviews.csv
```

The notebooks expect two columns:

- `text` — review text
- `label` — one of `fake_ai`, `fake_human`, `genuine`

Place the dataset in this folder before running the notebooks.
